{
  "name": "githelper",
  "displayName": "GitHelper",
  "description": "a GitHelper for simplylive.restore submodules,update all submodules,update all git(project) to head.",
  "version": "0.0.1",
	"publisher": "daiybh",
	"repository": "https://github.com/daiybh/githelper",
	"license": "MIT",
  "engines": {
    "vscode": "^1.77.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
		"git",
		"submodule",
		"helper"
	],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "githelper.init",
        "title": "githelper:init"
      },
      {
        "command": "githelper.ListSubModules",
        "title": "githelper:ListSubModules"
      },
      {
        "command": "githelper.restoreSubModule",
        "title": "githelper:restoreSubModule"
      },
      {
        "command": "githelper.pullALLCommand",
        "title": "githelper:pullALLCommand"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "test": "node ./out/test/runTest.js",
    "lint:fix": "eslint --fix src/**/*.ts"
  },
  "devDependencies": {
    "@types/vscode": "^1.77.0",
    "@types/glob": "^8.1.0",
    "@types/mocha": "^10.0.1",
    "@types/node": "16.x",
    "@typescript-eslint/eslint-plugin": "^5.56.0",
    "@typescript-eslint/parser": "^5.56.0",
    "eslint": "^8.36.0",
    "glob": "^8.1.0",
    "mocha": "^10.2.0",
    "typescript": "^4.9.5",
    "@vscode/test-electron": "^2.3.0"
  }
}
